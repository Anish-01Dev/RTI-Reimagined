# Infra

Deployment and infrastructure configuration. Local development uses `docker-compose.yml` in the repository root for PostgreSQL; deployment configuration is added here as the target environment is finalized.
